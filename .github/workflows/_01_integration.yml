name: 01 - Integration

on:
  pull_request:
    branches:
      - develop

jobs:
  Test:
    uses: ./.github/workflows/01-1_test.yml

  Quality_SonarCloud:
    needs: [Test]
    uses: ./.github/workflows/01-2_sonarcloud.yml
    secrets: inherit
